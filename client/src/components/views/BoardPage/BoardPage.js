import React,{useState, useEffect} from 'react'
import {Link} from "react-router-dom"

function BoardPage(props) {
    const Category = useState(props.match.params.CatID);

    return (
        <div>

        </div>
    )
}

export default BoardPage
